// Script assets have changed for v2.3.0 see
// https://help.yoyogames.com/hc/en-us/articles/360005277377 for more information
function region_get_block(bx, by, bz, region_folder){
	
	//Check if the chunk is open in memory using the chunks DS
	
	//If it isn't check if the chunk has been exported to a .mcc yet
	
	//If it hasn't,
	//Get region coordinates
	//open the region
	//open the chunk
	//open the section
	
	
	//get the block at position
	//return the ID and states
}